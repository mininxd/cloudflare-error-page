{
  "name": "refactor-project",
  "version": "1.0.0",
  "description": "A refactored project generating an HTML error page",
  "scripts": {
    "build": "node build.js",
    "test": "vite preview --outDir dist"
  },
  "devDependencies": {
    "ejs": "^3.1.10",
    "vite": "^6.0.0"
  }
}
